#include <iostream>
#include <algorithm>
#include <vector>
#include <iomanip>

int main() {
     int N, A, B;
     std::cin>>N>>A>>B;

     std::vector<int> nums(N);
    for (int i = 0; i < N; ++i) {
        std::cin>>nums[i];
    }
    int cnt=0;
    
    std::cout<<cnt;

    return 0;
}
